<Page x:Class="ArtCenter.DemoPages.UnderstandSizesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ArtCenter.DemoPages"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="40"
                   Foreground="Orange"
                   Text="Understand Sizes"
                   TextAlignment="Center" />
        <StackPanel Grid.Row="1">
            <TextBlock x:Name="SizeTextBlock"
                       FontSize="30"
                       HorizontalAlignment="Center" />
            <Button x:Name="MainButton"
                    Content="Example "
                    HorizontalAlignment="Center"
                    FontSize="30"
                    Margin="10"
                    Click="MainButton_Click" />
            <Image Source="ms-appx:///Assets/Robot01.png"
                   Stretch="Uniform" Height="400"/>
        </StackPanel>
    </Grid>
</Page>
